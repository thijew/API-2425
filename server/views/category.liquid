<h1>Categorie: {{ selectedCategory.name }}</h1>

<ul>
  {% for item in items %}
    <li>
      <img src="{{ item.icon }}" alt="{{ item.name }}">
      <strong>{{ item.name }}</strong><br>
      Type: {{ item.type }}
    </li>
  {% endfor %}
</ul>

<a href="/">← Terug naar alle categorieën</a>
