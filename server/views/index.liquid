{% layout "server/layouts/base.liquid" %}

{% block content %}

<h1>{{ title }}</h1>
<ul>
  {% for cat in categories %}
    <li>
      <a href="/category/{{ cat.id }}">{{ cat.name }}</a>
    </li>
  {% endfor %}
</ul>

{% endblock %}
